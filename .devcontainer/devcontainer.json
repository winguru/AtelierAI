{
  "name": "AI Image Curator",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "backend",
  "workspaceFolder": "/app",
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}/src:${containerWorkspaceFolder}/backend:${containerWorkspaceFolder}/dev:${PYTHONPATH}"
  },
  
  // Configure VS Code to install these extensions inside the container
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylance",
        "ms-python.black-formatter",
        "ms-python.flake8",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.linting.enabled": true,
        "python.linting.flake8Enabled": true,
        "python.formatting.provider": "black",
        "python.terminal.useEnvFile": true,
        "python.envFile": "${workspaceFolder}/.vscode/.env"
      }
    }
  },

  // Command to run after the container is created
  // "postCreateCommand": "pip install --upgrade pip",

  // Port to forward for the FastAPI application
  "forwardPorts": [8000]
}
