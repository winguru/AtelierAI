fastapi
uvicorn[standard]
sqlalchemy
pillow
python-multipart
exifread
python-dotenv
requests
wcwidth
ImageHash
blurhash
httpx

# CivitAI automatic authentication

playwright==1.52.0
playwright-stealth

# CLIP visual similarity
# ─────────────────────────────────────────────────────────────────────────────
# torch and torchvision are NOT pinned here because the correct wheel depends
# on the target OS/hardware.
#
# Install one of the platform-specific torch files before this requirements
# file:
#
#   Linux CPU-only:
#     pip install -r app/requirements.torch.linux-cpu.txt
#
#   Linux + NVIDIA CUDA:
#     pip install -r app/requirements.torch.linux-cuda.txt
#
#   Linux + AMD ROCm:
#     pip install -r app/requirements.torch.linux-rocm.txt
#
#   Raspberry Pi (ARM64):
#     pip install -r app/requirements.torch.raspberrypi.txt
#
#   macOS:
#     pip install -r app/requirements.torch.macos.txt
#
#   Windows:
#     pip install -r app/requirements.torch.windows.txt
#
#   Mixed environments: install torch FIRST from the correct index, then:
#     pip install -r requirements.txt
#
# The Dockerfile and dev container now handle this selection automatically.
# ─────────────────────────────────────────────────────────────────────────────
open_clip_torch
numpy
# torch and torchvision are pulled in as dependencies of open_clip_torch.
# If they are not already installed, pip will fetch GPU wheels from PyPI.
# For CPU-only targets, pre-install them from the CPU index before this file.